{
  "name": "sandpaper",
  "version": "0.0.1",
  "description": "Sand Claude Code's rough-draft HTML into a finished spec — refine the rendered document in place, no terminal round-trip.",
  "type": "module",
  "bin": { "sandpaper": "bin/cli.js" },
  "scripts": {
    "start": "node bin/cli.js sandpaper.html",
    "test": "node test/parse-test.js && node test/markdown-test.js"
  },
  "engines": { "node": ">=18" },
  "license": "MIT"
}
