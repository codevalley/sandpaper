# Log workflow

Use the remaining user arguments as the work summary. Prepend one `<li>` row to the top of both
`brain/log.html` and the cover's `<!-- BRAIN:LOG -->` feed. Use the next monotonic `w-NNNN` cid. The
row must be verb-led, no more than 12 words, and end in a link to a canonical anchor. Never edit a
prior row. Refresh the cover NOW region and `#brain-state` digest so the newest worklog line matches.
